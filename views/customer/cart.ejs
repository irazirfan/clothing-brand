<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cart Page</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f4f7f6;
            margin: 0;
            padding: 20px;
            color: #333;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        h1 {
            font-size: 36px;
            color: #007bff;
            margin-bottom: 20px;
        }

        p {
            font-size: 18px;
            margin-bottom: 20px;
        }

        /* General button/link styling */
        a {
            text-decoration: none;
            color: #007bff;
            font-size: 16px;
            padding: 8px 15px;
            margin: 5px;
            border: 1px solid #007bff;
            border-radius: 5px;
            transition: background-color 0.3s, color 0.3s;
        }

        a:hover {
            background-color: #007bff;
            color: white;
        }

        h3 {
            font-size: 24px;
            color: #333;
            margin-top: 30px;
            margin-bottom: 15px;
        }

        /* Style for the table */
        table {
            border-collapse: collapse;
            width: 80%;
            margin-bottom: 30px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            background-color: #fff;
        }

        th, td {
            border: 1px solid #ddd;
            padding: 10px;
            text-align: left;
        }

        th {
            background-color: #007bff;
            color: white;
            font-size: 18px;
        }

        td {
            font-size: 16px;
        }

        tr:nth-child(even) {
            background-color: #f9f9f9;
        }

        tr:hover {
            background-color: #f1f1f1;
        }

        /* DELETE button */
        .delete-btn {
            color: #dc3545;
            font-weight: bold;
            border: 1px solid #dc3545;
            padding: 5px 10px;
            border-radius: 5px;
            transition: background-color 0.3s, color 0.3s;
        }

        .delete-btn:hover {
            color: white;
            background-color: #dc3545;
        }

        /* Responsive design */
        @media (max-width: 768px) {
            body {
                padding: 10px;
            }

            h1, h3 {
                font-size: 28px;
            }

            table {
                width: 100%;
            }

            a {
                font-size: 14px;
                padding: 6px 12px;
            }
        }
    </style>
</head>
<body>

<h1>Shopping Cart</h1>

<table>
    <thead>
        <tr>
            <th>Name</th>
            <th>Category</th>
            <th>Price</th>
            <th>Action</th>
        </tr>
    </thead>
    <tbody>
    <% for(var i=0; i < cartList.length; i++){ %>
        <tr>
            <td><%= cartList[i].name %></td>
            <td><%= cartList[i].category %></td>
            <td>$<%= cartList[i].price %></td>
            <td>
                <a class="delete-btn" href="/customer/cart-delete/<%= cartList[i].id %>">DELETE</a>
            </td>
        </tr>
    <% } %>
    </tbody>
</table>

<!-- Back and Checkout buttons -->
<div>
    <a href="/customer">Back</a>
    <a href="#" class="checkout-btn">Checkout (beta)</a>
</div>

</body>
</html>
